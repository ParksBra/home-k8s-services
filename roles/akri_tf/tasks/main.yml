- name: Set requested templated values files variable
  ansible.builtin.set_fact:
    requested_values_templates_by_chart:
      - chart: akri
        fragments:
          - akri
        final_temp_path: "{{ temp_directory }}/akri_helm_values.yml"

- name: Run terraform
  ansible.builtin.include_role:
    name: terraform
  vars:
    tf_target_project: akri
    tf_state: planned
